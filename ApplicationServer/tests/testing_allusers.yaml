---
- config:
    - testset: "Testing allusers url"

- test:
    - name: "Get all users"
    - url: "/allusers"
    - method: "GET"
    - validators:
          - compare: {jsonpath_mini: "all_users.0", comparator: "eq", expected: 1}
          - compare: {jsonpath_mini: "all_users.1", comparator: "eq", expected: 2}
    - expected_status: [200] # ok
